package firststudy

/* 
	Map
	格式
		map_variable := make(map[KeyType]ValueType, initialCapacity)
		m := map[string]int{
			"apple": 1,
			"banana": 2,
			"orange": 3,
		}
		// 获取长度
		len := len(m)
		// 删除
		delete(m, "banana")
*/
func Test14() {

}